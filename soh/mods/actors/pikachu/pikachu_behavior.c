#include "pikachu_behavior.h"

void PikachuBehavior_Update(PlayState* play, Player* player) {
    // Superseded by the Pikachu transformation mask (pikachu_form.cpp).
    // Equipping the Keaton Mask now transforms Link into Pikachu via the
    // transformation_masks system. The floating-actor approach is deprecated.
    (void)play;
    (void)player;
}
